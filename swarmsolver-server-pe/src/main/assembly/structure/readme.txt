requirements

    this application needs a java JRE, version 17 or higher

configuration:
    set OPENAI_API_KEY in file data/config/keys.properties

running the application:

    On windows:

        run.bat

    On linux

        chmod u+x run.sh
        ./run.sh

